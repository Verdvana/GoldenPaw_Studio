# Environment Package Placeholder

Replace `_example` with a stable environment ID and copy `templates/environment/ENVIRONMENT.md`. Recurring fixed locations may become L1 after explicit approval and lock; episode-only environments remain L2. Neither can redefine characters or props.
