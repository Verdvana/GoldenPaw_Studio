# 资产状态规则

资产使用以下状态：

1. `source`：未经修改的原始来源材料
2. `working`：正在整理或测试
3. `candidate`：候选生产资产
4. `approved`：已审核，可用于正式制作
5. `deprecated`：停止用于新镜头，但旧镜头仍可引用

只有 `approved/` 中的资产可以作为正式生产的身份或外观基准。AI 生成图不得放入角色的原始来源目录。
